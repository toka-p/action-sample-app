name: Build action
on: [push]
jobs:
  Checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -lah
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: dotnet --version
      - run: dotnet publish -c Release -o ./publish
  Verifdy:
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah ./publish
        
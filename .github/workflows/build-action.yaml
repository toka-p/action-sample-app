name: Build action
on: [push]
jobs:
  Checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -lah
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: dotnet --version
        